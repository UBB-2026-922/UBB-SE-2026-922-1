<Page
    x:Class="BankingApp.Desktop.Views.BeneficiariesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:BankingApp.Application.DataTransferObjects.Beneficiaries"
    mc:Ignorable="d"
    Background="#F4F6FA">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="20" Margin="0,0,0,20">
            <TextBlock Text="Beneficiaries" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
            <Button Content="Add New Beneficiary" Click="ShowAddForm_Click" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Beneficiaries, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:BeneficiaryDataTransferObject">
                    <Grid Margin="0,5,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16" />
                            <TextBlock Text="{x:Bind IBAN}" Foreground="Gray" />
                            <TextBlock Text="{x:Bind BankName}" Foreground="Gray" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <Button Content="Use" Click="Use_Click" />
                            <Button Content="Delete" Click="Delete_Click" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,20,0,0"
                Visibility="{x:Bind ViewModel.IsAddFormVisible, Mode=OneWay}">
            <StackPanel Spacing="10">
                <TextBlock Text="Add New Beneficiary" FontWeight="SemiBold" FontSize="18" />

                <TextBox Header="Name" Text="{x:Bind ViewModel.NewName, Mode=TwoWay}" />
                <TextBox Header="IBAN" Text="{x:Bind ViewModel.NewIBAN, Mode=TwoWay}" />
                <TextBox Header="Bank Name" Text="{x:Bind ViewModel.NewBankName, Mode=TwoWay}" />

                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                    <Button Content="Save" Click="Save_Click" />
                    <Button Content="Cancel" Click="CancelAdd_Click" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
