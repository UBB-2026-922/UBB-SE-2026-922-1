@model BankingApp.Web.Models.Beneficiaries.BeneficiaryFormModel
@{
    ViewData["Title"] = "Add beneficiary";
}

<div class="page-header">
    <a asp-action="Index" class="text-muted-sm text-decoration-none">
        <i class="bi bi-arrow-left me-1"></i>Back to beneficiaries
    </a>
    <h1 class="page-title mt-2">Add beneficiary</h1>
</div>

<div class="form-card">
    <div class="card">
        <div class="card-body">
            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3"></div>

                <div class="form-group">
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control" placeholder="e.g. John Doe" />
                    <span asp-validation-for="Name" class="invalid-feedback d-block"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Iban" class="form-label"></label>
                    <input asp-for="Iban" class="form-control font-monospace"
                           placeholder="e.g. RO49AAAA1B31007593840000"
                           style="text-transform:uppercase"
                           oninput="this.value=this.value.replace(/\s/g,'').toUpperCase()" />
                    <span asp-validation-for="Iban" class="invalid-feedback d-block"></span>
                    <div class="form-text text-muted-sm">Spaces are removed automatically.</div>
                </div>

                <div class="form-group">
                    <label asp-for="BankName" class="form-label"></label>
                    <input asp-for="BankName" class="form-control" placeholder="e.g. ING Bank Romania" />
                    <span asp-validation-for="BankName" class="invalid-feedback d-block"></span>
                </div>

                <div class="divider"></div>

                <div class="d-flex gap-2 justify-content-end">
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-lg me-1"></i>Save beneficiary
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
